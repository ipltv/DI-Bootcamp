URL: https://github.com/ipltv/DI-Bootcamp/blob/main/Week6/Day2/ExercisesXP/all.sql
suggestions for improvement:
- In Exercise 1,  specify the table name 'items' and 'customers' to avoid ambiguity.  While context is clear here, it's good practice to always include the table name.
- For Exercise 2, question 8 and 9 assume a specific movie ('La La Land'). It might be better to make it more generic to allow students to use their own movie titles.  This would require using parameters in a real-world application but for this exercise, simply changing the example title makes it more reusable.
- In Exercise 2, question 11 (finding the next 10 cheapest movies without LIMIT), the use of `ROW_NUMBER()` is efficient, but consider adding an index on the `rental_rate` column for improved performance.  This is not a readability or correctness issue, but a performance enhancement based on the method chosen.
- Add comments to the Exercise 2 queries to explain the purpose of each query more explicitly.  This enhances readability significantly.
Brief justification:
- correctness: The code directly and accurately addresses all the SQL queries outlined in both Exercise 1 and Exercise 2. Each query fulfills the specified requirements, correctly manipulating data and producing the expected results as described in the chapter content.  The queries demonstrate understanding of basic SQL commands like SELECT, WHERE, ORDER BY, LIMIT, JOIN, DISTINCT, and subqueries. The bonus questions are also correctly answered.
- readability: The code is generally readable.  The SQL syntax is clean and easy to follow. However, adding table names (as noted above) and comments would greatly enhance readability, particularly for larger or more complex queries. Using more descriptive alias names might also be beneficial in certain queries.
- performance: The performance of the individual queries is generally good for the small datasets likely used in this exercise. However, the lack of indexing (as noted above) could become a bottleneck in a production environment with larger tables. The use of `ROW_NUMBER()` in the 11th query of Exercise 2 shows good understanding of a technique to solve the problem without `LIMIT`, though optimizing with an index would further enhance this. The queries are not overly complex, so performance is not a major issue for this exercise.
- security: The code does not present any obvious security vulnerabilities.  It's assumed data inputs are controlled and the queries are run in a secure database environment, which would be the normal context for such exercises.

